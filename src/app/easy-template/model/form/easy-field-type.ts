export enum EasyFieldType {
  id = 'id',
  text = 'text',
  number = 'number',
  decimal = 'decimal',
  date = 'date',
  autoComplete = 'autoComplete',
  select = 'select',
  multiSelect = 'multiSelect',
  email = 'email',
  password = 'password',
  file = 'file',
  checkbox = 'checkbox',
}
